# Environment Variables for Co-Work

# Google Gemini API key for server-side AI features
GEMINI_API_KEY=

# Optional: API base URL override (defaults to same origin in dev & prod)
VITE_API_URL=

# Optional: Base path for Vite assets (defaults to ./)
VITE_BASE_PATH=
